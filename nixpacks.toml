[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npm run build", "cd backend && npx prisma generate"]

[start]
cmd = "cd backend && npm run start:prod"

[staticAssets]
outputDir = "backend/dist"
